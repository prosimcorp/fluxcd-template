---
apiVersion: source.toolkit.fluxcd.io/v1beta2
kind: GitRepository
metadata:
  name: flux-system
  namespace: flux-system
spec:
  url: git@github.com:{{ YOUR_COMPANY_NAME }}/{{ AUTOMATED_CLUSTER_NAME }}.git
  ref:
    branch: master
