apiVersion: source.toolkit.fluxcd.io/v1beta2
kind: GitRepository
metadata:
  name: tooling-stack
  namespace: flux-system
spec:
  interval: 20s
  url: https://github.com/prosimcorp/tooling-stack.git
  ref:
    branch: master
  secretRef:
    name: flux-system
